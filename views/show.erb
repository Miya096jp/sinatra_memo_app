<h2>MEMO</h2>

<p class="title"><%= h(@memo["title"]) %>:</p>
<p class="article"><%= h(@memo["body"]) %></p>

<div class="btn_show">
  <a href="/edit/<%= params[:id] %>" class="btn">Update</a>

  <form method="POST" action="/<%= params[:id] %>">
    <input type="hidden" name="_method" value="DELETE">
    <input type="submit" value="Delete" class="btn">
  </form>
</div>
