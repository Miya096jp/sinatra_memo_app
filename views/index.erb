<h2>MEMO</h2>
<ul class="memo-list">
  <% @memos.each do |key, value| %>
    <li class="memo">
      <a href="/<%= key.to_i %>"><%= h(value['title']) %></a>
    </li>
  <% end %>
</ul>
<div class="button">
  <a href="/new" class="btn">Create New Memo</a>
</div>
